{
  "name": "@forgesf/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./types": "./src/types.ts"
  },
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "migrate:reset": "node scripts/migrate.js --reset",
    "generate": "node scripts/generate-types.js",
    "build": "echo 'No build needed for migrations'",
    "lint": "echo 'No lint needed for migrations'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "supabase": "^1.191.3",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
